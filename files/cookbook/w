# SOURCE
# START HELP
# navigate to the workspace directory, supports subdirectory navigation 
# END HELP
if [ ! -d ~/workspace ]; then
    mkdir -p ~/workspace
fi

local target_path path_element
target_path="$HOME/workspace"
for path_element in "$@"
do
  if [[ "$path_element" = "--complete" ]]; then
    ls $target_path
    exit
  fi
  target_path="$target_path/$path_element"
done

echo $target_path
cd $target_path
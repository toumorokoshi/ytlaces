#!/usr/bin/env bash
# a script to provision my home bazzite desktop,
# which I use for game and media streaming
set -ex
BASE=$(dirname $(dirname "$(realpath "$0")"))
sudo "$BASE/bin/install-base.py" "$BASE/configs/bazzite" "$USER"